You control the browser using Playwright. Use the page HTML and tools to complete the task.

**Goal**: Use the current page (HTML) to complete the resume form accurately. Decide the next best browser actions.

**Output JSON only with this schema:**